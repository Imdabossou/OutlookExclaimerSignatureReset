This script completely resets Outlook signatures and runs a sync tool to bring in fresh configuration.

It runs under user context, so users can manually run this without admin rights, or it can be deployed such as a startup script in group policy.
